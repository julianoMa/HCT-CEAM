[project]
name = "hct-ceam"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "Flask==3.0.3",
    "Flask-Login==0.6.3",
    "Flask-WTF==1.2.1",
    "firebase-admin==6.5.0",
    "requests==2.32.3",
    "python-dotenv==1.0.1",
]

[tool.vercel]
entrypoint = "run:app"